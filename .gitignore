# 本仓库技术栈：Go + Vue/Vite/TS + Tauri(Rust) + Node/pnpm + Playwright。
# 规则只保留与该栈相关的项；通用模板里属于其它框架（Next/Nuxt/Gatsby/SvelteKit/
# VitePress/Docusaurus/Serverless/Bower/jspm/Yarn 等）的条目已移除。

# ---- 依赖与构建产物 ----
node_modules/
dist/
*.tsbuildinfo

# ---- 日志与工具缓存 ----
*.log
.eslintcache
.vite/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.pnpm-store/

# ---- 覆盖率（前端 istanbul / 后端 go cover）----
coverage
*.lcov
coverage.out
coverage.txt

# ---- 环境变量 ----
.env
.env.*
!.env.example

# ---- Rust / Tauri ----
# cargo 的 registry 缓存（桌面端构建时生成于仓库根）
.cargo/

# ---- s3clinet ----
# Go server（`data/` 与 `node_modules/`、`dist/` 已由上方通用规则覆盖，不再重复列出）
apps/server/s3clinet-server

# 本地演示/截图产物
.shots/

# Tauri desktop
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
apps/desktop/src-tauri/icons/source.png

# Playwright
apps/web/playwright-report/
apps/web/test-results/
apps/web/blob-report/
apps/web/playwright/.cache/

# 运行数据（含明文账号密钥，禁止提交）
data/
.run/

# Agent 本地覆盖文件（个人偏好，不提交）
AGENTS.local.md
CLAUDE.local.md

# Agent 工具本地目录（个人配置，不提交）
.claude/
.cursor/

# gitlab-ci-local：状态目录与本地变量文件不提交；`.gitlab-ci-local-env`（默认 VOLUME）与
# `.gitlab-ci-local-variables.yml.example` 是团队约定，**要提交**。
.gitlab-ci-local/
.gitlab-ci-local-*.yml

# 编辑器 / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.iml
*.swp
*.swo
*~

# 操作系统垃圾文件（本项目同时发布 macOS .dmg 与 Windows .exe）
.DS_Store
Thumbs.db
Desktop.ini
