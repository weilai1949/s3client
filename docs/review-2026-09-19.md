# 分支整体状态审查（2026-09-19）

> **审查对象**：`develop` @ `0fbd560`（`git describe` = `v1.0.0-rc1-110-g0fbd560`，工作区干净）
> **审查范围**：架构 / 正确性 / 测试与质量门禁 / 安全 / 性能 / 文档 / 可发布性
> **方法**：全部门禁**实跑复现** + 四路独立深度审查（后端 Go、前端 Vue/TS、CI 与发布链、文档 SSOT）
> × 关键发现逐条人工复核。**对子审查结论不做背书**——本轮推翻了 2 条（见 [附录 A](#附录-a--被推翻的结论)）。
>
> **证据图例**：🟢 实跑复现 · 🔵 源码可证（含调用链） · ⚪ 仅代码阅读推断（未实跑） · ❌ 已否证
>
> **与既有文档的关系**：本文件记录 `develop` @ `0fbd560` 时点的审查结论，不回写、不覆盖
> [`assessment.md`](assessment.md)（2026-09-16 五维度评估）的历史结论。
> **已闭环的发现项直接从本表移除**（不留处置痕迹），归档证据见 [`features.md`](features.md) §P 与
> [`CHANGELOG.md`](../CHANGELOG.md) `[Unreleased]`；仍开放的缺陷汇总见
> [§9](#九风险汇总与后续优先级)。

---

## 〇、执行摘要

**总体判断：工程质量显著高于平均。本轮登记的全部 P0 与 [§三 正确性](#三阶段-2--正确性)（B3–B11 / F2–F10）
均已清零**，剩余发现集中在响应契约门禁、文档失真、发布链与安全（见 [§9.2](#92-p1--尽快发布后首个补丁) / [§9.3](#93-p2--规划须与文档同-pr)）。

正面结论**不是**纸面声明——本轮逐条实跑复现：`go vet` / `golangci-lint` 0 告警，8/8 包
`-race` 通过且**每包 100% 语句覆盖**（`count==0` 零块），前端 986 例 / 64 文件 + 四指标 100%，
`govulncheck` 0 可达漏洞，`pnpm audit` 0 漏洞，`cargo audit` 0 漏洞（7 条上游无修复的
unmaintained/unsound 告警），**RustFS 真对端 E2E 4/4 通过**，Playwright 15 通过 / 0 跳过，
10 个 action 固定 SHA 全部经 GitHub API 核验为真。架构分层、ADR 兜底、风险登记与归档台账的
完备程度属于行业标杆水平。

**本轮的方法论价值在于「门禁全绿 + 覆盖率 100% + 台账全 ✅」的保护色下仍找出了真缺陷**——
这些缺陷全部诞生在同一类盲区：**分支/语义正确但断言缺席**（已关闭 channel 的零值语义、
不可信存储的数据形状、跨前缀的 key 映射、buildx `load` 默认值、注册表与 handler 的字段级漂移）。
对应的机械门禁已随修复一起补上，使同类回归会直接让门禁变红。

**仍需人工跟踪的元结论**：风险登记表的 ✅ 与实际断言范围之间仍可能再有落差——
[§4.3](#43-契约门禁的实际断言范围根因分析) 列出的**响应契约门禁**（handler DTO ⇔ OpenAPI schema
双向比对）至今不存在，是 §7.2 与 §7.4 最大缺口的根因，也是本轮唯一未补上的门禁类型。

---

## 一、阶段 0 · 门禁实测基线

以下结果均为本轮**实跑**（环境：go1.26.6 / node v26.5.0 / pnpm 9.15.0（由 `packageManager` 固定）
/ docker / cargo-audit 0.22.2）。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| Go 格式 | `gofmt -l .`（`apps/server`） | 🟢 干净 |
| Go 静态 | `go vet ./...` | 🟢 0 告警 |
| Go Lint | `golangci-lint run ./...`（v2.13.2，与 CI 同版本） | 🟢 **0 issues** |
| Go 测试 | `go test -race -count=1 ./...` | 🟢 8/8 包通过（handler 包 153s） |
| Go 覆盖率 | `-coverprofile` + `awk '$NF==0'` | 🟢 **每包 100.0%**，无零计数块 |
| Go 构建 | `go build ./...` | 🟢 通过 |
| Go 漏洞 | `govulncheck ./...` | 🟢 0 可达（21 条位于未调用模块） |
| 前端 lint | `pnpm lint`（`eslint src --max-warnings 0`） | 🟢 0 error / 0 warning |
| 前端类型 | `vue-tsc --noEmit` | 🟢 exit 0 |
| 前端测试 | `pnpm test` | 🟢 986 例 / 64 文件全绿 |
| 前端覆盖率 | `pnpm test:coverage` | 🟢 四指标 100%（阈值 100 生效） |
| 前端构建 | `pnpm build` | 🟢 358.06 kB（gzip 109.90 kB） |
| npm 审计 | `pnpm audit` | 🟢 0 已知漏洞 |
| Rust 审计 | `cargo audit` | 🟢 0 漏洞；恰 7 条 unmaintained/unsound 告警 |
| **真对端 E2E** | RustFS `1.0.0-rc.3` + `S3CLINET_E2E=1 go test ./internal/s3wrap/ -run TestE2E` | 🟢 **4/4 PASS**（版本 / 12 MiB 分段 / 批量 / 桶配置 / 回收站） |
| **浏览器 E2E** | Playwright chromium（`pnpm e2e:ci`） | 🟢 15 passed / 0 skipped |
| 供应链 | 10 个 action SHA 逐一查 GitHub API | 🟢 **10/10 HTTP 200** |
| 文档链接 | 25 个 md 相对链接 | 🟢 0 断链 |

**结论**：既有文档中「门禁全绿」的声明**全部属实**。但这层绿色不能推出系统健康——
见 [§4](#四阶段-3--测试与质量门禁深挖) 的盲区分析。

---

## 二、阶段 1 · 架构与分层

- 单向依赖 `handler → service → s3wrap`、`handler/store → model` 成立，无循环依赖。
- `s3wrap` 是 AWS 类型与外界**唯一**边界；handler 与前端无 AWS 类型泄漏。
- 无文件突破 1000 行红线：前端最大 `composables/useObjectActions.ts`(601)，`src/api/` 目录内
  最大单模块 `storage.ts`(339)；后端最大 `openapi.go`(476) < `service/job.go`(468) < `objects.go`(465)。
- 关键决策有 ADR 兜底且与代码一致：存储硬失败（ADR-002）、SSRF 放行私网（ADR-003）、
  桌面端无 IPC（ADR-001）、前端仅依赖 vue（ADR-004）。

---

## 三、阶段 2 · 正确性

本节登记的后端 B3–B11 与前端 F2–F10 已**全部闭环**并从本表移除（本文件按约定只保留未闭环发现，
不留处置痕迹）；逐条归档证据见 [`features.md`](features.md) §R 与 [`CHANGELOG.md`](../CHANGELOG.md) `[Unreleased]`。
每条修复都配了会先失败的回归测试，覆盖的正是本轮的方法论盲区——**分支/语义正确但断言缺席**：
S3 在 200 响应体内的逐 key `<Error>`、无缓冲 channel 的消费者退出、`Emit`/`Finish` 的锁外竞态、
8 MB 上限与 10 000 key 承诺的落差、段号 10000 的边界差一、`filename*` 的 RFC 5987 编码、
`NextContinuationToken` 不前进时的死循环、不可信 `<Code>` 的指标标签基数。

---

## 四、阶段 3 · 测试与质量门禁深挖

### 4.1 正面：门禁真实、可执行、无消音

- 全仓无 `continue-on-error` / `|| true` / `set +e`；`allow_failure: true` 只出现在
  `when: manual` 的任务上（等价于 GitHub 侧被跳过的 `desktop-build`）。
- `if: always()` / `if: failure()` 只用于产物上传与清理，**从不用于门禁**。
- `golangci-lint-action` 的 `only-new-issues` 默认为 false → GitHub 的 lint 门禁是**全仓**，
  与 GitLab 侧 `golangci-lint run` 一致。
- 覆盖率门禁检查 profile 中 `count==0` 的语句块，而非 1 位小数的百分比——避免了 99.96% 被
  四舍五入成 100.0% 而漏过回退。

### 4.2 但覆盖率的「形状」掩盖了缺陷

- **本轮 P0 中有 4 个在 100% 语句覆盖下存活**。语句覆盖率对本项目的缺陷类型
  （分支语义、数据形状、跨源字段映射）**不敏感**。
- 21 个前端测试名**硬编码源码行号**（如 `api.test.ts:1251,1265,1286`），且行号**已经过期**
  （实际 `??` 在 `api/storage.ts:49`/`:196`，非数组回退在 `:185`）——测试在描述一个不存在的版本，
  属实现耦合而非行为断言。
- `i18n/coverage.test.ts:69-81` 把**注释里的**键形字符串也算"已使用"；`:140-143` 只比对**键数量**
  做双语对称 → 盲区存在（当前实测 0 死键、670/670 对称，暂未造成漏检）。
- 全部 `/api/**` 被 `page.route` mock（`features.spec.ts:322`）→ **不存在"真实 Go 后端 + 真实前端
  产物"的联调门禁**；前后端 DTO 契约只靠单测与契约测试保障。

### 4.3 契约门禁的实际断言范围（根因分析）

| 门禁 | 实际只断言 | 因此漏掉 |
|---|---|---|
| `api_doc_test.go:225` `TestAPIDocDocumentsRequestBodyFields` | **单向** `OpenAPI ⊆ docs`，且用 `strings.Contains(body, name)` 子串匹配（`:239`） | ① 文档侧的幻影字段从不检查；② 子串碰撞（字段 `key` 被 `keys`/`secretKey` 满足，`newKey` 被 `newKeys` 满足）；③ query 参数、响应字段、类型、required |
| `openapi_contract_test.go:850` `...RequestBodyMatchesHandlers` | **硬编码 7 个端点族**（2026-09 修过的那批） | 其余全部端点（"注册表/文档多写了 handler 不解析的字段"这类反向漂移尚无全量门禁） |
| `openapi_inputsource_test.go:165` | 只比对"有无 `readJSON(`" | 字段名、类型、required |
| `api_doc_test.go:68` `TestAPIDocMatchesRoutes` | 路由集合双向 diff；解析正则只匹配**行首** `METHOD /api` | 行内 / 缩进 / 反引号写法的失效端点 |
| `deadcode_gate_test.go:47` | 仅 `^_ = ident` 与 `^var _ = expr` 两种形状 | 其余死代码（交予 golangci-lint `unused`） |

**关键缺口：没有任何门禁把「响应 schema」与「handler 的真实 DTO」做比对**。请求体侧也只有
**逐端点族**的字段集断言（`openapi_contract_test.go` 第 1–8 项），没有「注册表声明字段集 ⇔ handler
结构体字段集」的全量遍历。因此"字段加进注册表和文档、但忘了加进 handler"这一类改动，
**在新端点上仍可以 100% 全绿通过**。

---

## 五、阶段 4 · 安全

### 5.1 已核实的强项（读实现 + 实跑，非文档声明）

| 威胁 | 缓解 | 证据 |
|---|---|---|
| 鉴权仿冒 | Bearer 常量时间比较（sha256 + subtle）、scheme 大小写不敏感、多 token 轮换、最短 16 字符 | `middleware.go:221-245`、`config.go:15` |
| CSRF | 非白名单 Origin 直接 403 阻断"简单请求"；强制 `application/json` 使跨域变更必预检 | `middleware.go:144-175`、`handler.go:180-196` |
| SSRF | 创建期 + **拨号期二次解析逐 IP 校验**（消除 DNS 重绑定 TOCTOU）+ 禁重定向 + 禁代理 | `s3wrap/ssrf.go:34-129` |
| 敏感数据 | `AccountView` 不含 secretKey；错误脱敏；S3C2/S3C3 信封 + Argon2id(≥t=2) + 原子写 0600 | `model/account.go:43-60`、`store/crypto.go` |
| 单副本 | `store.AcquireDataDirLock`（unix flock），第二实例启动即失败 | `store/lock.go`、`main.go:73-78` |
| XSS | 生产代码 0 处 `v-html`/`innerHTML`/`eval`；预览走服务端代理 + 类型白名单 | 全量 grep |
| 供应链 | 10 个 action SHA **全部经 GitHub API 核验为真**；`pnpm audit` 0；`govulncheck` 0 可达；`cargo audit` 0 漏洞 | 见 [§1](#一阶段-0--门禁实测基线) |

### 5.2 风险

| # | 风险 | 证据 | 等级 |
|---|---|---|---|
| S1 | **运行镜像基础版 `alpine:3.20` 已于 2026-04-01 EOL**，而 Trivy 以 `--ignore-unfixed` 运行 → 在 EOL 分支上"所有未来 CVE 永远无修复版"，安全门禁**结构性失明**。endoflife.date 实测：3.20 eol = `2026-04-01`；当前受支持线为 3.21/3.22/3.23/3.24 | `Dockerfile:44`、`ci.yml:160`、`.gitlab-ci.yml` | **高** |
| S2 | **`.env.example` 的占位 token 是"有效口令"**：`change-me-use-openssl-rand-hex-32` 长 33 ≥ `MinTokenLength=16`，满足 compose 的 `${S3C_TOKEN:?}` 守卫 → 照 README 快速开始 `cp .env.example .env && docker compose up -d` 会以**公开已知 token** 上线 | `.env.example:6`、`docker-compose.yml:40`、`config.go:15` | **高** |
| S3 | base compose 默认 `sqlite` + 空 `S3C_STORE_KEY` → secretKey 明文落盘（仅启动 WARN；`prod.yml:31` 才强制） | `docker-compose.yml:35-36` | 中 |
| S4 | **`/api/metrics` 在设置 `S3C_TOKEN` 时仍免鉴权**（实测：无 token → 200）。设计如此，但文档未明说"即使配了 token 也不受保护" | `middleware.go:217` | 中 |
| S6 | 明文落盘（R3）只降级为 WARN，且 `S3C_STORE_KEY` 无"非默认值"校验 | `config.go:208-221` | 低 |

> **S4 实测**：`S3C_TOKEN=<32字符>` + `S3C_EXPOSE_METRICS=1` 下，
> `/api/health`→200、`/api/metrics`→**200（无 token）**、`/api/openapi.json`→401、`/api/accounts`→401。

> 原 S5（指标标签取服务端原始 `<Code>`，基数无界）已随 [§三](#三阶段-2--正确性) B10⑥ 关闭：
> `errorClass` 改为 20 个已识别错误码的白名单，其余归 `other`（测试 `TestErrorClassUnknownCodeFoldsToOther`）。

---

## 六、阶段 5 · 性能

### 6.1 已核实的良好设计

流式 32 并发 + 滚动空闲写超时（`stream.go`）；批量 4 worker + 无缓冲结果通道（`batch.go`）；
`partBuf` 池上限 1×64 MB（**可证明有界**，`stream_copy.go:20-23`）；list/index 10 万硬上限；
前端 `entries` 单次排序（`useObjectBrowser.ts:96-111`，历史重复排序已消除）；全仓无 `deep:` watcher；
grid 视图 300 节点上限并提示（`ObjectList.vue:86-88`）。

### 6.2 问题

| # | 问题 | 影响 |
|---|---|---|
| P3 | `/api/openapi.json` 每次请求全量重新 marshal 70 个 operation（无缓存，虽默认 404） | 低–中 |

> 本节原有的 P2（[§三](#三阶段-2--正确性) F3 / F7 的无界并发扇出）与 P3 前半句（B6 `indexDst` 无页数上限）
> 已随 §三 关闭：presign 走 4 路有界池、迁移/删除按服务端上限分片，`indexDst` 与 `listAll` 共用
> 页数/总量硬上限并每轮检查 ctx。仅保留 `/api/openapi.json` 的全量 marshal 一条。

---

## 七、阶段 6 · 文档与 SSOT

### 7.1 已核实为真

- **端点数三重双射**：`routes.go` = **70** = OpenAPI 注册 **70** = `docs/api.md` **70**，
  且 `openapi_contract_test.go:40` 的常量也是 70；双向集合 diff 无缺无余。
  文档中所有"70 个端点"的说法正确（`roadmap.md:33`、`README.md:225`、`api.md:668`）。
- **193 个相对文档链接 0 断链**（24 个文件）。
- **CHANGELOG 纪律良好**：`[Unreleased]` 覆盖最近 15 个提交无遗漏；已发布区自 `f5a9ef1` 起
  **字节未变**（无追溯篡改）。
- **roadmap §四 的全部数字经重新测量正确**：8/8 包、每包 100%、986 测试 / 64 文件、
  `-race` 通过、govulncheck 0 可达、cargo audit 0 漏洞 + 恰 7 条告警、10 个 SHA pin、
  dependabot 5 生态、Go 1.26.6 三处一致、SDK 版本、RustFS 版本、`.trivyignore` 确为空清单。
- 抽查端点字段一致：`POST …/mkdir` 三处均为 `key`（注册表 `openapi_register_objects.go:94-103` /
  handler `objects.go:152-154` / 文档 `api.md:159`）——2026-09 的 R1 首轮修复对该端点是**真的**。

### 7.2 响应契约失真（P1）🟢

`components.schemas.Account`（`openapi.go:379-395`）与真实响应 DTO 不符。实测真实响应：

```json
GET /api/accounts → {"id","name","endpoint","publicEndpoint","region","accessKey",
                     "secretSet","bucket","pathStyle","useSSL","createdAt","updatedAt"}
```

| schema 声明 | 实际情况 |
|---|---|
| `provider` | `model.Account` 无此字段；请求侧幻影字段已从注册表与文档删除 |
| `forcePathStyle` | **全仓仅出现在该 schema 自身**（`openapi.go:392`） |
| `insecureSkipVerify` | **全仓仅出现在该 schema 自身**（`openapi.go:393`） |
| — | **漏掉真实的 `useSSL`**（`model/account.go:52`，实测响应中存在） |

该 schema 是 `GET /api/accounts`、`POST /api/accounts`、`GET|PUT /api/accounts/{id}` 的
200/201 响应 —— **所有账号端点的响应契约都是错的**。

### 7.3 其余文档失真

| # | 问题 | 证据 | 等级 |
|---|---|---|---|
| D1 | `s3wrap/metrics.go:52` 注释称 Latency 为「**累积**直方图」，实现是「每次调用只 +1 个桶」（`:121`），测试亦如此断言（`metrics_test.go:127-151`）；而 `handler/metrics.go:129-136` 却按 Prometheus 直方图语法输出 `_bucket{le=...}`。**实测输出违反 Prometheus 契约**（`le` 必须单调不减，且 `+Inf` 必须等于 `_count`）：`le="0.01"`=3、`le="+Inf"`=0、`_count`=3 → `histogram_quantile()` 结果全错 | 复现见 [附录 B](#附录-b--关键复现命令) | 中 |
| D2 | `openapi_handler.go:7` 注释称该端点"**不经鉴权层**"——**实测相反**：无 token 访问 `/api/openapi.json` → **401**（`withAuth` 仅豁免 health/metrics）。注释错误（实际行为更安全） | 实测 | 低 |
| D3 | 发布脚本 `release-version.sh:16-19` 正则 `^v1\.0\.0-...$` **拒绝纯 `v1.0.0`**（roadmap §三 的首个稳定里程碑）与 `v1.1.0`/`v1.0.1`；且漏更新 `docs/deployment.md:101`、`docs/roadmap.md:17`、`docs/features.md:9`、`.github/ISSUE_TEMPLATE/bug_report.md:36`。`CONTRIBUTING.md:40` 称"同步 8 处"，实际为 **12 文件 13 处** | 实测正则 + 逐文件核对 | 中 |
| D4 | `features.md` 的「已修复」声明仍有一处与代码不符：`failKeys ≤200`（`batch.go:82` 不裁剪，10k 全失败会回 ~10 MB 并落盘 `jobs.json`）。**另两处已核实闭环**：T-5「prefix 过滤 ✅」由 [features.md](features.md) §Q #3 的 `TestMigrateSync_PrefixFilter` 补上，P-3「zip goroutine 泄漏 ✅」由 [§三](#三阶段-2--正确性) B4 真正修好（此前 `break` 仍泄漏） | `features.md` | 中 |
| D5 | roadmap 称 `v1.0.0` 已收口（2026-09-17），但 **`git tag v1.0.0` 不存在**，且 HEAD 距 `v1.0.0-rc1` 已 **110 个提交**、11 处版本串仍为 `1.0.0-rc1` | `git describe` | 中 |
| D6 | `docs/api.md:520`（及 `features.md`）写 presign `expiresIn`「范围 1s–7 天…默认 15 分钟」，代码是**默认 1h、上限 24h**（`objects.go:434-441`）；`architecture.md:67` 反而是对的 → 同一参数两种说法 | 各文件 | 中 |
| D7 | `storage-class` 的真实 `versionId`（`objects.go:105`）未在注册表声明；4 个 query 参数未声明：`head.versionId`、`proxy.maxBytes`、`trash.prefix`（文档有）与 **`objects.startAfter`（文档也没有——一个未公开的公开特性）** | 各文件 | 中 |
| D8 | `README.md:16`、`architecture.md:101`、`features.md:62`、`features.md:439` 四处写"3 路并发"上传，实际 `UPLOAD_CONCURRENCY = 2`（`useUploadQueue.ts:10`，2026-09-04 改为 2 后文档未同步） | 各文件 | 低 |
| D9 | `docs/development.md:127` 自相矛盾：同一段先声明 `CHANGELOG.md` 是根目录约定文件，随后又写"其余文档（**含 `CHANGELOG.md`**）统一放 `docs/`"；实际在根目录，`AGENTS.md` 与前半句一致 | `development.md:127` | 低 |
| D10 | `CHANGELOG.md` 的 `[Unreleased]` 段内两处过期数字：覆盖率计数（3883/2769/1059/3339 vs 实测 3943/2797/1076/3393）、"983 测试"（实测 984） | `CHANGELOG.md` | 低 |

### 7.4 契约漂移的门禁盲区矩阵

| 漂移类型 | routes↔registry | registry↔handler 字段 | doc⊆registry 字段 | doc 端点集 | 死代码门禁 | 能否拦截 |
|---|---|---|---|---|---|---|
| 端点增删 / 改名 | ✅ | ✅（仅存在性） | — | ✅ | — | **能** |
| 请求字段只在 handler 改名 | ✅ | ❌ | ❌ | — | — | **否** |
| 字段加进注册表+文档、未加进 handler | ✅ | ❌ | ✅（绿） | — | — | **否** |
| 字段在 handler、注册表缺失 | ✅ | ❌ | ❌ | — | — | **否** |
| query 参数错误 / 缺失 | ✅ | ❌ | ❌ | — | — | **否** |
| **响应 schema 与 DTO 不符** | ✅ | ❌ | ❌ | — | — | **否** |
| 类型 / required / 枚举语义（presign 除外） | ✅ | ❌ | ❌ | — | — | **否** |
| md 表格中的叙述性数字 | — | — | — | — | — | **否** |
| `_ = ident` / `var _ = expr` | — | — | — | — | ✅ | **能** |
| 其他死代码（测试/导出/间接） | — | — | — | — | 部分（golangci `unused`） | **部分** |

---

## 八、阶段 7 · 可发布性

### 8.1 发布阻断项

| # | 结论 | 证据 | 等级 |
|---|---|---|---|
| R2 | **无 tag ↔ 清单一致性门禁**：`release-desktop.yml:44-66` 解析 tag 后从不与 `tauri.conf.json` / `Cargo.toml` 比对，今天打 `v1.0.0-rc2` 会发布**标着 rc1** 的安装包与镜像 tag | `release-desktop.yml:44-66` | **P0** |
| R3 | 三平台矩阵并行 `gh release upload SHA256SUMS.txt --clobber` 同一资产名 → **最后写入者胜**，发布出去的校验清单只覆盖一个平台 | `release-desktop.yml:123-137` | 高 |
| R4 | Trivy DB 下载无缓存 / 重试 / 镜像 pin，仓库自带日志记录过 `FATAL … connection timed out`（6m44s 后失败）；本轮本地复现亦超时 | `.gitlab-ci-local/output/docker.log:113-115` | 高 |
| R5 | Trivy 固定在 `0.58.1`（落后 16 个小版本），且该版本已对 CI 依赖的 `--vuln-type` 打 deprecation 警告 | `ci.yml:155` | 中 |
| R6 | Rust 工具链未 pin（`dtolnay/rust-toolchain@…# stable` + `rust:1-bookworm`，无 `rust-toolchain.toml`）；`apps/desktop/package.json` 缺 `packageManager`（本地 pnpm 11.7 vs CI 9.15）；Node 只 pin 大版本 | 各文件 | 中 |
| R7 | 本地 `make` ≠ CI：无 `lint` / `golangci-lint` / `govulncheck` 目标；`make` 用裸 `pnpm install` 会改写锁文件；`test-all` 无覆盖率门禁；`test-cover` / `install-hooks` 不在 `.PHONY` | `Makefile` | 中 |
| R8 | `.dockerignore` 漏 `apps/web/coverage`、`.pnpm-store`、`test-results`、`.run/`、`.cargo/` 等约 5.4 MB → **同一 commit 因本地是否跑过覆盖率而产出不同镜像层** | `.dockerignore` | 中 |
| R9 | GitHub 构建不传 `VERSION` build-arg（回落到过期字面量），GitLab 传 `VERSION=ci`，Makefile 传实版本 → 三种行为一个 Dockerfile | `ci.yml:133-141` vs `.gitlab-ci.yml:246` | 低 |
| R10 | 无任何 workflow 推送镜像到 registry（`push: false`）→「发布」目前只有 tag + 桌面安装包 | `ci.yml:138` | 低 |

### 8.2 已核实为合格的发布要素

- 部署产物：`prod.yml` 强制 `S3C_STORE_KEY:?` + `S3C_TOKEN:?`；nginx 单 worker + 128M 内存上限；
  容器非 root（`USER app`，实测 uid=1000）；`HEALTHCHECK` 指向免鉴权的 `/api/health`
  （`Dockerfile:61`、`routes.go:14`、`middleware.go:217`）；多阶段构建正确，前端 `dist` 确实进入镜像。
- **RustFS 真对端 E2E 可运行且门禁正确**：`e2e_test.go:19-22` 的 `S3CLINET_E2E` 开关有效，
  本轮实测 4/4 PASS；compose 与 GitLab service 的镜像/凭据/端点一致。
- 锁文件齐全且 CI 冻结（`--frozen-lockfile` / `--locked` / `go.sum`）；Go 工具链三处一致（1.26.6）。
- GitHub / GitLab 的触发语义与 `development.md:61-68` 逐条吻合。

---

## 九、风险汇总与后续优先级

### 9.1 总体判断

工程质量**显著高于平均**：门禁不是装饰（本轮逐条复现全绿）、供应链 pin 全部真实、真对端 E2E 与
浏览器 E2E 均可跑通、架构分层与决策台账完备。

**本轮登记的全部 P0 已随修复清零**（每项都配了会先失败的回归测试 / 变异验证），因此结论从
「不可发布」改为：**P0 准入条件已满足**。已闭环项按本文件约定从表中移除，证据归档在
[`features.md`](features.md) §Q（P0 与架构项）/ §R（§三 正确性）与 [`CHANGELOG.md`](../CHANGELOG.md) `[Unreleased]`。

**[§三 正确性](#三阶段-2--正确性)（B3–B11 / F2–F10）已全部处置**（2026-09-19 当轮完成，与 P0 同一批修复）：
剩余未闭环的发现集中在**响应契约门禁缺失**（[§4.3](#43-契约门禁的实际断言范围根因分析) / [§7.2](#72-响应契约失真p1-)）、
**文档失真**（[§7.3](#73-其余文档失真)）、**发布链**（[§8.1](#81-发布阻断项)）与**安全**（[§5.2](#52-风险)）。

**仍要人工跟踪的系统性风险**是「风险登记表的 ✅ 与门禁实际断言范围之间的落差」：

- **R1（契约漂移）被宣告「已收敛为自动化门禁」**，而门禁至今看不见**响应级**漂移
  （[§7.2](#72-响应契约失真p1-)）——请求体字段级已补上针对性断言，响应 schema 仍无门禁。
- **其余未闭环缺陷同样诞生在"分支/语义正确但断言缺席"的盲区里**；
  补门禁（而非只修缺陷）才是本轮真正的产出——[§三](#三阶段-2--正确性) 的每条修复都按此执行。

### 9.2 P1 — 尽快（发布后首个补丁）

- **§7.2** 修正 `components.schemas.Account`，并**新增响应契约门禁**（handler DTO ⇔ schema 双向比对）。
- **§4.3** 把 `api_doc_test.go` 的字段门禁改为**双向**并去掉子串匹配；把
  `openapi_contract_test.go:850` 的硬编码 7 端点族改为全量遍历。
- **R2** 增加 tag ↔ 清单一致性检查；**R3** `SHA256SUMS` 改为平台内唯一命名 + 聚合 job；
  **R4** Trivy DB 缓存 / 重试；**S1** 升级到受支持的 alpine（3.21+ / 3.22）；**S2** `.env.example`
  的 token 置空（`apps/server/.env.example` 已是正确写法，可直接对齐）。

### 9.3 P2 — 规划（须与文档同 PR）

- **D1** 直方图：要么改为真累积，要么改用 `_bucket` 之外的正确语义（同步修注释与测试）。
- **D2** 修注释；**D3** 修发布脚本（放宽版本正则 + 补齐漏更新文件）；**D5** 打 tag 或修正 roadmap 状态。
- **D6 / D7 / D8 / D9 / D10** 文档数字与承诺修正。
- 测试质量：把 21 个行号测试名改为行为描述；补一条**真实后端 + 真实构建产物**的浏览器冒烟
  （当前完全缺失）。
- S3 / S4 / S6、R5–R10。

### 9.4 建议的验证闭环

1. 在 100% 覆盖率之外补**响应契约门禁**（handler DTO ⇔ OpenAPI schema 字段双向比对）→
   治 §7.2，并封堵 §7.4 最大缺口。
2. 把 `deadcode_gate_test.go`、`api_doc_test.go`、`openapi_inputsource_test.go` 的断言范围在文件头
   注释里写清（"本门禁不覆盖 X"），避免下一轮评估再次把"有门禁"误读为"该类风险已收敛"。
3. 请求体字段级门禁目前是**逐端点族硬编码**（`openapi_contract_test.go` 第 1–8 项）：新端点写错
   字段仍不会自动变红，建议改为「注册表声明字段集 ⇔ handler 结构体字段集」的全量遍历比对。

---

## 附录 A · 被推翻的结论

审查过程中对下级结论逐条复核，以下 **2 条经复核不成立**，记录在此以免误修：

| 原结论 | 复核结果 | 证据 |
|---|---|---|
| 「`copy.go:190-193` 移动路径静默失败，报移动完成但源仍在」 | ❌ **不成立**。该处使用单选 `DeleteObject`（会返回错误），并以 `ErrSourceDeleteFailed` sentinel 上抛（`copy.go:192`），批量结果按 `errors.Is` 识别。B3 的真实受影响点是 4 处 `DeleteObjects`，**不含**移动路径 | `s3wrap/object.go` 调用点全量 grep |
| 「`UploadPart` 的 ETag 去引号后传给 `CompleteMultipartUpload`，在真机上很可能失败」 | ❌ **在 RustFS 上不成立**。临时用例把 `multipartPartSize` 设为 5 MiB、12 MiB 载荷经 SDK `CreateMultipartUpload → UploadPart → Complete` 走真实 RustFS：**3 段往返成功**（`OK: SDK multipart path round-tripped 12582910 bytes through 3 parts`）。仍属"其他厂商一致性"的待观察项，**不是 bug** | 实跑（临时用例已删除） |
| 「虚拟列表钳制的收敛需数千轮」 | ⚠️ **量级夸大**。方向正确（陈旧 `scrollTop` 导致 0 行已由源码与组件级测试确证），但收敛轮数取决于列表长度，本报告不背书该具体数字 | 复核 `windowed` 计算与浏览器钳制模型 |

---

## 附录 B · 关键复现命令

```bash
# ---- 全门禁 ----
cd apps/server && go vet ./... && golangci-lint run ./... && govulncheck ./...
go test -race -count=1 -coverprofile=coverage.out ./... && \
  awk 'NR>1 && $NF==0 {print "UNCOVERED: "$0; bad=1} END{if(bad) exit 1}' coverage.out
cd apps/web && pnpm lint && pnpm typecheck && pnpm test && pnpm build

# ---- 真对端 E2E（RustFS）----
cp .env.example .env && docker compose up -d rustfs
cd apps/server && S3CLINET_E2E=1 S3CLINET_ENDPOINT=http://127.0.0.1:9000 \
  S3CLINET_ACCESS_KEY=rustfsadmin S3CLINET_SECRET_KEY=rustfsadmin \
  go test ./internal/s3wrap/ -run 'TestE2E' -v -count=1

# ---- 浏览器 E2E（须先起 preview，CI 同款两步式）----
cd apps/web && pnpm build && (pnpm preview --port 4173 &) && \
  CI=1 PLAYWRIGHT_NO_WEBSERVER=1 pnpm e2e:ci

# ---- D1：Prometheus 直方图契约违规 ----
curl -s http://127.0.0.1:8080/api/metrics | grep s3c_s3_call_duration_seconds
#   le="0.01" 3 / le="+Inf" 0 / _count 3   ← 违反 le 单调性与 +Inf==_count

# ---- 供应链：action SHA 核验 ----
for r in $(grep -rh 'uses:' .github/workflows/*.yml | sed 's/.*uses: //;s/ #.*//' | sort -u); do
  curl -s -o /dev/null -w "%{http_code} $r\n" \
    "https://api.github.com/repos/${r%@*}/commits/${r#*@}"; done
```

---

## 附录 C · 审查环境与还原声明

- **未修改仓库任何文件**；本文件是唯一新增内容。
- 审查期间使用的临时探针（前端组件探针、Go overlay 测试、临时 E2E 用例）、`.env`、
  RustFS 容器、探针服务与 `/tmp` 目录**均已清理**；`git status` 干净，无残留容器与监听端口。
- **局限**：① 本环境 Docker Hub 不可达，R1 的本地复现退化为上游 `action.yml` 默认值核验
  （结论仍确定，因为 `load` 与 `driver` 默认值来自固定 SHA 的 `action.yml`）；
  ② Trivy 实际扫描未跑（R4 的 DB 下载在本环境超时）；③ 部分性能结论基于静态分析而非基准测试；
  ④ `docs/api.md` 的锚点式链接（`#...`）未校验。
